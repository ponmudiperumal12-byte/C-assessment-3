#include <stdio.h>
int main() {
    int a;
    printf("Enter the number: ");
    scanf("%d", &a);
    if (a >=50) 
    {
        printf("success");
    } else 
    {
        printf("failure");
    }
}
