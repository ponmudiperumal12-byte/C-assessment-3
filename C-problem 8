#include <stdio.h>
int main() {
    int a, f, s;
    printf("Enter the two digit number: ");
    scanf("%d", &a);
    f=a%10;
    s=a/10;
    if (f!=s) 
    {
        printf("success");
    } else 
    {
        printf("failure");
    }
}
